# Handoff -- 2026-08-06

> **STATUS 2026-08-07: epic 7oo is closed. All ten children are fixed and
> verified against the real machine.** Read this file for the reasoning that
> produced them; the work itself is done. What is left from this list:
>
> * `7oo.11` (new, P2) -- genuine in-app disable/remove for startup items.
>   `7oo.4` resolved the honesty problem (the panel now states that it only
>   reports, and every orphan names where its kind of entry is managed); doing
>   it in-app is the follow-up.
> * The parked stages (`bfh`, `be8`, `ag0`, `0ng`) are now unblocked. The P0 gate
>   they were waiting behind has closed.
> * The sandbox checklist (`69a`, `kt0`, `1qp`, Win11 half of `0xt`) still needs
>   a human at the console. Unchanged.
>
> The gate that was missing is now `test/real-data-verify.js`. Run it before
> believing any suite total on this project:
>
> ```
> npx electron test/real-data-verify.js
> ```
>
> As of 2026-08-07 on the operator's machine: 152 passed, 0 failed across
> eleven sections, plus 262/0 for the geometry sweep and 314/0 for `npm test`.


Written at the end of a long multi-day session, immediately after an
operator audit of the real running app produced this verdict:

> "we have an aesthetic product with poor ux and minimal functionality,
> this is the worst of all worlds. implode the fluff, build a robust
> software."

That verdict is correct. This document exists so the next session starts
from it rather than rediscovering it.

## Start here

1. `bd prime` (auto-loads; the persistent memory has the short version)
2. `bd show vanish-uninstaller-7oo` -- the epic that owns the real work
3. This file, for the why

Do NOT start by reading the roadmap. Its Standard-tier stages are parked
(see below) and reading them first reproduces the exact mistake that
caused this handoff.

## What actually happened

Prior sessions fixed defects one at a time, each time the operator
reported one. Each individual fix was real, tested, and verified. The
aggregate result was still a broken product, for two compounding reasons:

**1. Fixture-shaped testing validated fixture-shaped reality.** Every UI
test in this repo runs against `test/fixtures/stub-preload.js`: one fake
app, clean well-formed fields, instant responses. The real machine has
163 apps of varied shape, multi-second scans, partial states, Windows
component entries, and a scrollbar. The suite reported 312/312 passing
while the app was visibly broken on screen. Most damning specific case:
`test/ui-interaction-verify.js` exists *specifically* to catch buttons
that render but cannot be clicked (its own header documents a prior
occlusion defect) -- and it passes green while real action buttons in the
uninstall wizard sit occluded on the operator's screen.

**2. New scope was added on top of a foundation that did not hold.**
This same session scoped three new roadmap stages (15 bandwidth panel,
16 firewall sweep, 17 Windows Update rollback) plus assorted re-scoping
work, while the already-shipped surfaces were substantially non-functional.
The scoping was individually defensible. The sequencing was indefensible.

Both failure modes are already documented as general patterns in
`D:\Depthworks\Projects\ideations\agent-verification-pitfalls\RETROSPECTIVE.md`
(AGP-1 especially). They were written down twice during this session and
still not applied where it mattered. Read that file before doing
diagnostic or verification work here.

## The work, in order

Everything below lives under epic **`vanish-uninstaller-7oo`**.

**Binding rule on the epic: no child closes on a stub-test pass alone.**
Every one of these defects is invisible to the stub suite by
construction. Verification means the real app with real data (the
operator's machine, or the Sandbox).

### P0 -- do these first

| Issue | What |
|---|---|
| `7oo.10` | **Real-data test harness.** The prevention item; arguably do this first so the rest are provably fixed. Must reproduce at least two existing defects *before* they are fixed -- that is the proof it would have caught them. |
| `7oo.1` | Uninstall wizard action buttons occluded by another element |
| `7oo.2` | Force Uninstall lists wrong/un-actionable entries AND omits real ones |
| `7oo.3` | System-component filter protects every Microsoft product, not just what Windows needs (Office/Teams/Edge must be uninstallable) |

### P1

| Issue | What |
|---|---|
| `7oo.4` | Orphaned/startup entries shown with buttons that do not truly resolve them, no suggestions |
| `7oo.5` | System Clean full-rescans on every interaction, no incremental update, shows partial counts as final |
| `7oo.6` | Redundancy detection treats same-family entries (Edge + Edge Update + WebView2) as duplicate installs |
| `7oo.7` | App list needs a Windows Features (optional features) toggle |
| `7oo.8` | System overview does not span the screen and shows no storage at all |
| `6g2` | No scan progress anywhere (pre-existing; closely related to `7oo.5`) |

### P2

`7oo.9` -- quarantine details readable but not user-friendly. Operator
explicitly called this "adequate," lowest severity on the list.

## Parked, deliberately

Stages 15/16/17 (`bfh`, `be8`, `ag0`) and Stage 11 (`0ng`) are parked
with notes on each. They are well-scoped and stay filed -- the scoping
work is not wasted -- but starting any of them before `7oo`'s P0 children
close would repeat the exact error that produced this handoff. Rule 16's
Core-VM gate applies on top of that, independently.

## Strategy change (new, affects priorities)

Operator confirmed: **B2B paid, personal use free.** This supersedes the
MS Store paid Convenience Edition decision recorded on
`vanish-uninstaller-22n` and `adrs/0001-stack-override.md`. Filed as
`vanish-uninstaller-2c6` (needs a superseding ADR).

Consequence that matters more than the paperwork: a paying business will
not tolerate a single item on the `7oo` list. The quality bar just moved,
which is another reason robustness now outranks new stage scope.

## Still blocked on a human, unchanged

`test/sandbox/vanish-sandbox.wsb` + `test/sandbox/VERIFICATION-CHECKLIST.md`
still need an operator to run them (UAC prompts require a human at the
console by design; no agent-side workaround exists or should be built).
That closes `69a`, `kt0`, `1qp` and the Win11 half of `0xt`. The sandbox
now auto-installs Chrome via winget so there is a real app with real
leftovers to test against.

This is lower priority than it was: the `7oo` defects are reproducible on
the operator's own machine without any sandbox, and are worth more.

## State at handoff

- git `76e5db3`, clean, pushed
- 312/312 tests passing -- **which this session proved is not the signal
  it appears to be**; treat that number with suspicion until `7oo.10`
  exists
- bd: 51 issues, 26 closed
- `gh` active account: `QuantumMonkey`. The ideations repo needs
  `DepthWorks` (`gh auth switch --user DepthWorks && gh auth setup-git`,
  then switch back) -- see the `gh-account-gotcha` memory
